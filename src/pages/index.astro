---
import { getCollection } from 'astro:content';
import MainLayout from '@layouts/MainLayout.astro';

let essays = await getCollection('essays');
essays = essays.sort((a, b) => {
  if (new Date(a.data.pubDate) < new Date(b.data.pubDate))
    return 1;
  if (new Date(a.data.pubDate) > new Date(b.data.pubDate))
    return -1;
  return 0;
});
---

<MainLayout
  title="Home | Jose Vigil"
  description="Jose Vigil's personal and professional website"
  image=""
>
  <h1 class="dark:text-white">
    I'm Jose!
  </h1>
  <section class="grid grid-cols-1 gap-5 lg:grid-cols-3 lg:gap-8">
    <div class="lg:col-span-2">
      <p class="mb-3">
        Lorem ipsum dolor sit amet, consectetur adipisicing elit. Assumenda tempora sunt iste quia officia illo obcaecati esse alias, nihil corrupti animi cupiditate adipisci temporibus aliquam quaerat vitae culpa, vel omnis.
      </p>
      <p class="mb-3">
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Delectus rerum laborum facilis corrupti aliquam praesentium. Voluptate, provident! Esse culpa omnis nesciunt quo vitae excepturi alias reprehenderit, temporibus odio modi doloribus.
      </p>
      <hr>
      <h2 class="dark:text-white">
        Essays
      </h2>
      <div class="essays">
        <ul>
          {
            essays.map(e => (
              <li>
                <a href={`/essays/${e.slug}`} title="">{ e.data.title }</a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
    <div class="order-first md:order-last">
      <img src="https://i.pravatar.cc/300" class="rounded-full m-auto" alt="Jose Vigil" />
      <p class="text-lg dark:text-white text-center mt-3">Creative, living my dreams</p>
    </div>
  </section>
</MainLayout>
